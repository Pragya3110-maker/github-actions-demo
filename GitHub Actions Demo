name: GitHub Actions Demo

on:
  push:
    branches:
      - feature-workflow

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello GitHub Actions!"
